name: hello world

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Displays hello world message
        run: echo "Hello World!"
        
      - name: Another Task
        run: echo "Another Task"  
